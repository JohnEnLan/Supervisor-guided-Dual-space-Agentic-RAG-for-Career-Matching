# Career RAG Load Validation Report

**Date:** 2026-07-14

**Branch:** `codex/week3-reoptimization`

## Scope and Safety

This report covers the local P0/P1-mechanism prototype with deterministic database, LLM, and embedding substitutes. Pytest disables dotenv loading before application modules are collected and forces synthetic database/provider settings; tests did not read or print `.env`, did not call DeepSeek or DashScope, and used only synthetic session, run, job, case, and evidence identifiers.

The measurements are a local engineering baseline. They demonstrate state isolation, async overlap, recovery contracts, and bounded provider concurrency; they are not internet-scale capacity claims and do not predict real provider latency.

## Complex Business Scenarios

The focused matrix executed 48 tests in 1.64 seconds:

- Targeted Intent Agent consultation with a named role or company.
- Explore consultation with at most three directions and no redundant second Agent call after selection.
- One bounded clarification followed by locked Match Brief fields.
- Canonical `plan_hash`, stale-plan 409 response, and structured recovery guidance.
- Result projection requiring JD evidence and rejecting hidden or hard-constraint-failing jobs.
- Public explanation with bounded case evidence and no raw state, prompt, provider error, or full resume.
- Dual-space cold start, confident reranking, implicit failure degradation, and cancellation propagation.
- Concurrent run snapshots, result isolation, status polling, and competing execute behavior.

Result: **48 passed, 0 failed**. One pre-existing Starlette/httpx deprecation warning was reported.

## Concurrent Run Isolation

Twenty synthetic runs were executed concurrently across ten session IDs. Even-numbered runs used a targeted career goal; odd-numbered runs used an exploration goal. Each run produced its own job ID and JD evidence ID.

| Metric | Result |
|---|---:|
| Runs | 20 |
| Successful | 20 |
| Failed | 0 |
| Success rate | 100% |
| Peak concurrency | 20 |
| P50 | 11.397 ms |
| P95 | 12.893 ms |
| Maximum | 13.111 ms |
| Local harness throughput | 1,422.900 runs/s |

Within the injected run-keyed persistence substitute, no session ID, run ID, result snapshot, job ID, or evidence ID crossed into another run. The test now fails if run-scoped execution attempts to write shared `session_state`; production run progress is persisted to `match_runs.state_snapshot` by `run_id`. This proves the run snapshot/result contract, not real PostgreSQL contention or concurrent feedback persistence. The high throughput reflects fully substituted I/O and must not be presented as real LLM throughput.

## Read API Load

The FastAPI application was exercised in-process with `httpx.ASGITransport`. Two hundred `GET /api/v1/runs/{run_id}/status` requests used twenty async concurrency slots.

| Metric | Result |
|---|---:|
| Requests | 200 |
| HTTP 200 | 200 |
| HTTP 5xx | 0 |
| Success rate | 100% |
| Peak concurrency | 20 |
| P50 | 12.800 ms |
| P95 | 20.168 ms |
| Maximum | 76.807 ms |
| Local harness throughput | 628.845 requests/s |

Every response returned the run ID requested by that coroutine.

At the API contract boundary, twenty simultaneous execute requests for the same run produced exactly one HTTP 202 and nineteen recoverable HTTP 409 responses. The injected queue substitute deliberately models one winner, so this verifies route behavior and conflict mapping; it is not evidence of a real PostgreSQL atomic race.

## Retrieval Parallelism

The retrieval tests use event rendezvous points as deterministic evidence of overlap, plus a wall-clock guard:

- SQL/metadata hard filtering completes first and produces the allow-list.
- BM25 and dense branches each wait at a two-party rendezvous before completing, so a sequential implementation cannot pass.
- Explicit job retrieval and implicit case retrieval use the same rendezvous contract.
- A 500 ms `asyncio.wait_for` guard detects deadlock; machine-dependent millisecond thresholds are not CI pass/fail criteria.
- Implicit ordinary exceptions degrade to explicit results.
- Explicit retrieval failures remain run failures.
- `asyncio.CancelledError` propagates instead of being hidden by degradation.

The focused retrieval/provider suite result was **21 passed, 0 failed**.

## Provider Semaphore Limits

Provider clients were replaced before invocation, so no network request or API charge occurred.

| Provider boundary | Calls | Configured test limit | Observed peak |
|---|---:|---:|---:|
| DeepSeek chat | 5 | 2 | 2 |
| Qwen embedding | 9 | 3 | 3 |

Both tests proved that calls can overlap while never exceeding their configured semaphore.

## P50, P95, Throughput, and Failures

The statistics use nearest-rank percentiles and are generated by `app.evaluation.load_validation.summarize_latencies`. Empty samples, excessive success counts, and negative peak-concurrency inputs are bounded deterministically.

The complete backend regression result was **211 passed, 0 failed in 4.26 seconds**. The complete frontend checks produced:

- OpenAPI generated-type check: passed.
- Vitest: 12 files, 22 tests passed.
- TypeScript project check: passed.
- Vite production build: passed; 1,844 modules transformed.
- Playwright Chromium: 13 end-to-end tests passed in 5.9 seconds.

## Defects Found and Resolved

- The new root route originally rendered the upload workbench and invoked capabilities. The isolated `/` and `/workspace` route test failed first, then passed after route separation.
- The introduction initially focused its heading on first load, producing an oversized focus frame. A regression test now requires no initial focus while preserving focus transfer after step changes.
- Load tests originally inherited the default `.env` configuration. `CAREER_RAG_ENV_FILE=""` now disables dotenv for tests, and collection forces synthetic provider settings before application imports.
- Run-scoped orchestration originally wrote intermediate state into shared `session_state`. It now persists every stage snapshot by `run_id`, and the concurrency test rejects shared-session writes.
- The execute race originally used an injected lock while the report described it as production evidence. It is now documented only as an API conflict-mapping contract.
- Strict 180 ms retrieval assertions could fail on a busy machine despite proven overlap. Rendezvous assertions remain the concurrency proof, with only a broad deadlock timeout.
- On Windows, Playwright-owned Vite shutdown can delay command completion. Verification now reuses an explicitly managed test server; this changes test process management only, not application behavior.

## Remaining Production Limitations

- The load suite does not measure a real PostgreSQL/pgvector server under network and disk contention.
- The production `queue_run` SQL transition has not been race-tested against a real PostgreSQL instance; its one-winner behavior still needs an authorized staging database test.
- It does not consume real DeepSeek or Qwen quotas, so provider latency and rate-limit recovery require a separately authorized staging test.
- Execution durability remains `process_local`; a process restart can interrupt background execution even though snapshots are stored by run ID.
- Measurements are from one Windows development host, not a multi-instance deployment behind a load balancer.
- These tests validate mechanism correctness and local async behavior, not recommendation quality on a labelled career-matching dataset.
