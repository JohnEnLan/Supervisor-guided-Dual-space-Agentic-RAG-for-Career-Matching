# B4 首日真实冒烟记录：qwen-vl-ocr（2026-08-09）

按 v3 方案 §4"首日真实冒烟确认模型名/参数"条款执行。发生于 M1 提交
ff14e0f **之后**（故该提交信息写 smoke deferred，以本记录为准）。

## 方法

- 客户端：`app/llm/qwen_vl.py::ocr_image_jpeg`（ff14e0f 版本）——DashScope
  OpenAI 兼容 endpoint，`model=qwen-vl-ocr`，消息体为 data-URL JPEG +
  固定指令 `Read all the text in the image.`。
- 输入：Pillow 合成 640×200 白底黑字 JPEG（q70，8165 字节），四行中英混排
  简历片段（姓名/联系方式占位/实习经历/学历）。
- 执行：本机 venv，真实 API 调用一次（约分钱级）。

## 结果：PASS

模型完整返回全部四行文本，逐行语义无误（含 "ZHANG SAN - Data Analyst"、
"2023 ByteDance internship: SQL, Python, A/B testing"、
"University of Birmingham MSc Data Science 2024-2025"）。
断言 token 命中：`['ZHANG', 'Birmingham', 'SQL']`（≥2 判 PASS）。

## 结论

- 模型名 `qwen-vl-ocr` 与 OpenAI 兼容 chat.completions 调用方式实证可用，
  M1 契约（data-URL + 固定指令）无需调整。
- M2b 管线可直接建立在 `ocr_image_jpeg(jpeg_bytes) -> str` 之上；
  离线测试一律 mock 该函数，不再耗真实调用。
