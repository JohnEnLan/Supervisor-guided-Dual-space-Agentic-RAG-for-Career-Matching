> **【历史盘点，已被 `docs/master_execution_plan.md` v5 取代，非执行依据】**
> 本文批次编号与工期口径（13.5–16d）已过时；执行以 master v5 为唯一入口。

# 整合执行总方案（一次执行版 · 2026-08-06 盘点）

> 用途：把本轮全部沟通产出的"已批未执行"事项整合为一个可一次执行的总方案。
> 盘点口径：以 git 提交与评审记录为准，不凭记忆。

## 一、今日已执行清单（无需再动，仅供对账）

| 事项 | 提交 | 状态 |
|---|---|---|
| 咨询 LLM 重试 / 局域网 Cookie 开关 / 3 会话付费墙 / embedding 缓存 | 9cf49cf | ✅ 已上线 |
| RAPTOR 两缺陷修复 + 全量索引 31,886 节点 | 9d518f1 | ✅ |
| 演示语料评估管线 + 四通道消融（raptor_cross P@5 0.920 / MRR 1.000） | 2dc2097 / 57a0db3 | ✅ |
| Cross-encoder 精排全实现（四轮双审方案 → 516 tests） | 6df5296 | ✅ |
| RAPTOR_ENABLED + RERANK_ENABLED 双开（彩排 3/3） | dbe27c5 | ✅ |
| 功能与代码详解 Word（双审）/ 不足与展望 / 三 persona 彩排套件 | 5ba6112 等 | ✅ |

## 二、已批/在批但未执行的工程方案（本总方案的主体）

### 工作流 W-A：前端产品化 P0（docs/frontend_ux_redesign_plan.md v3.2）
- 评审状态：**三方通过**（子 agent 三轮 + Codex 六轮 APPROVE）；
- 内容：P0-0 现网缺陷包（反馈 422 / cancelled 终态 / run 持久化 / 终态出口 /
  PM 去重 / fixture 契约）→ 群公告进度卡 → 状态指示 → 槽位 chips → 消息质感 →
  结果区 → 滚动保护 → 导航额度空态 → 简历档案展开 → 角色主动性（前端部分）→
  移动导航；
- 独立工期 3–3.5 天，零 OpenAPI 变更。

### 工作流 W-B：生产增强 P0（docs/production_enhancement_plan.md v10 终稿）
- 评审状态：**三方通过**（子 agent 六轮 + Codex 六轮，双 APPROVE，
  30+ 项意见收敛，9 条实施注收录方案 §10）；
- 内容（终版设计要点）：A 简历澄清回路——结构化质量问题触发 ≤2 问、
  **pending question 锚点制**回答归属、证据铁律（span.text=用户原话逐字、
  C### 服务端定 ID）、确认基线不可变 + 三 helper 消费闭环（含召回 query
  builder）、**隐式匿名空间零消费澄清**（隐私最强形态）、上传 202 窗口 +
  重确认洞的 **generation 生命周期**封堵（新列 resume_upload_generation +
  preview/confirm 收紧 + 409 恢复旅程）；B PM 咨询督导——L1 每轮确定性
  事实 + L2 有界教练 ≤3 次（**reservation 状态机**：预算原子预留、crash
  烧掉、幂等追加），PM 消息内嵌当轮转录条目 supervisor_notes（零破坏
  DTO），fail-open 铁律，verdict 双落 supervisor_log；
- 独立工期 **10–12 天**（全加性变更 + 两个默认关的环境开关，关闭即
  运行时行为等价，四组合 00/10/01/11 测试锁定）。

### 工作流 W-C：文档对齐（今日盘点发现的**未下单欠账**）
早前问题清单的 A.1（三本主文档过时）在后续指令中未被点名，至今未执行：
- README.md：仍描述已删除的 Examiner View 与旧 e2e 覆盖；
- docs/code_guide.md：仍讲已删除的 onboarding 电影页、"314 项测试"（现 516）；
- docs/product_guide.md：页面清单仍是"职业远征"旧落地页；
- 论文 docx（毕业设计介绍 v7.1）：描述停在 V2 之前（群聊/认证/咨询/LangGraph/
  语料/精排全部缺失）——**答辩硬伤级**；
- 附带：本轮新增能力（澄清回路、PM 督导、双开检索）落地后需回写
  功能与代码详解。
- 工期约 1–1.5 天（README+两本 guide 以功能详解为蓝本重写；论文 docx 需你
  给写作口径后另排）。

## 三、明确不在本次执行范围（防丢失，逐项留档）

| 类别 | 事项 | 去向 |
|---|---|---|
| 两方案 P1 | 暗色模式（两套设计系统统一）、双栏任务板、动态能力胶囊（per-run applied 字段）、匹配强度条（explain）、精确额度与标题落库、同会话重开咨询/run 历史、run-input API（执行 agent 真提问）、真重归一化（re-confirm 流）、澄清进长期记忆、SSE 真流式、ProfilePage chips 化、landing 缩影 | 答辩后按价值排期 |
| 有意暂缓 | known_issues 13 条（案例库双模型、账号删除、进程内队列等） | 论文"不足与展望"素材 |
| 运营项（你的动作） | QQ 授权码答辩前轮换；短信服务商选定后我接线；云服务器购买决策（香港节点免备案建议）；tmp/pt* 管理员手动删 | 等你指令 |

## 四、整合执行序（关键：两方案有交叉件，合并后省重复工）

两方案在 **P0-9 角色主动性**上交叉（v3.2 的前端插话 ↔ 生产方案的真实 PM
条目），合并执行按依赖排序：

```
批次 1  W-A P0-0 现网缺陷包 + typed fixture（一切的地基，先红后绿）
批次 2  W-A 群公告进度卡 + 状态指示 + 滚动保护
批次 3  W-A 槽位 chips + 消息质感
批次 4  W-A 结果区重构 + 简历档案展开 + 额度/空态
批次 5  W-B-A 简历澄清回路（后端：结构化质量问题保留 → 触发器 →
        resume_clarify 阶段 → origin 证据回填 → DTO/快照/类型再生）
批次 6  W-B-B PM 咨询督导（后端：L1/L2 + supervisor_log + 转录扩展）
批次 7  角色主动性合并件（v3.2 P0-9 ∪ 生产 §3 衔接表）：
        前端渲染真实 PM 转录条目 + 保留即时插话 + 第四枚澄清 chip +
        运行期投影交接确认（v3.2 P0-9B）+ 结果后行动 chips
批次 8  W-A 移动抽屉导航 + 移动冒烟
批次 9  W-C 文档对齐（README/两本 guide 重写 + 功能详解回写）
批次 10 总验收：全量 pytest + 前端四门 + e2e 全场景 + 真机彩排
        （P1–P3 三 persona + 新增 P4 简历含糊者）+ 32 项全局冒烟复跑
```

每批次四门检查（typecheck/vitest/build/e2e + 后端 pytest/pyflakes）全绿才进
下一批；每批一个提交、双远端推送。

**合并后总工期估计：13.5–16 个工作日**（W-A 3–3.5 + W-B 10–12 + W-C 1–1.5，
减去 P0-9 交叉件合并与只跑一轮总彩排省下的 ~1 天）。W-B 六轮评审把工期
从最初 5.5d 如实上修到 10–12d——增量全部来自评审揪出的真问题（竞态矩阵
六面封死、generation 生命周期、reservation 状态机、消费闭环精确到函数），
不是范围膨胀。若需压缩：W-B 有已评审的压缩版 ≈ 7–8d（砍断档检测/教练
转换类触发/A-B 基础设施，见方案 §6，需你明确选择）。

## 五、执行前置条件

1. ~~W-B 评审收敛~~ **已收敛**（v10 终稿，双 APPROVE；批次 5–7 按终稿
   设计执行：批次 5 = A 澄清回路 + generation 生命周期，批次 6 = B PM
   督导 reservation 状态机，批次 7 = 角色主动性合并件 + 409 恢复旅程）；
2. 你对本总方案的批次顺序/取舍确认（尤其 W-C 是否纳入、论文 docx 是否
   本轮动、W-B 全量 10–12d 还是压缩版 7–8d）；
3. 执行分工沿用既定流程：每批次 Claude 或 Codex 执行 → 对侧审查 → 我终验。
